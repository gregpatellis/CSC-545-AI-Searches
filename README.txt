The program was written in Python 3.6. Main.py can be compiled by itself with dependencies on polygon.py, vector2d.py and environment.py. The program holds the same structure as
the template given, with minor adjustments to environment.py. Addtional methods have been implemented in main.py utilized by the search algorithms. The A* and Greedy searches both
return the same path.
